// One contract on both sides of the private transport; no duplicate validator.
export { selectedConversationRequest, validateSelectedConversation } from '../extension/selected-conversation-contract.mjs';
