# Local runtime boundary

Reserved for CLI, coordinator, native host, protocol, SQLite state, collection,
publication, verification, configuration and safe-path modules.

The [implementation plan](../docs/implementation-plan.md) is authoritative.
There is no working CLI or worker in this scaffold; no module may default tests
to a live browser, home state or Miyo service.
